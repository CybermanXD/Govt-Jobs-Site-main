const esc = value => String(value ?? '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function loadJobDetails(){const p=new URLSearchParams(location.search),id=p.get('id'),url=p.get('url'),title=p.get('title')||'Job Details';document.getElementById('breadcrumbs').innerHTML=`<a href="/">Home</a> &gt; ${esc(title)}`;const query=id?`id=${encodeURIComponent(id)}`:`url=${encodeURIComponent(url||'')}`;try{const r=await fetch(`/api/job_details?${query}`),d=await r.json();document.getElementById('job-detail').innerHTML=`<h2>${esc(d.postName||title)}</h2><p><strong>Company:</strong> ${esc(d.companyName||'N/A')}</p><p><strong>Posts:</strong> ${esc(d.noOfPosts||'N/A')}</p><p><strong>Qualification:</strong> ${esc(d.qualification||'N/A')}</p><p><strong>Salary:</strong> ${esc(d.salary||'N/A')}</p>${url?`<p><a href="${esc(url)}" target="_blank" rel="noopener noreferrer">View source</a></p>`:''}`;}catch(e){document.getElementById('job-detail').innerHTML='<p>Details are temporarily unavailable.</p>';}}
document.addEventListener('DOMContentLoaded',loadJobDetails);
